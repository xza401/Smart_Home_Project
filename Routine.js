class Routine {
  constructor(id, userId, schedule, deviceId, action) {
    this.id = id;
    this.userId = userId;
    this.schedule = schedule;
    this.deviceId = deviceId;
    this.action = action;
  }
}
export default Routine;
